/**
 * Allows for reading variable properties from local.properties and exporting them as variables
 */
def localProperties = new Properties()
localProperties.load(new FileInputStream(rootProject.file("local.properties")))

ext {
    /**
     * The Google Play License key hash
     */
    LICENSE_KEY_HASH = localProperties.get("LICENSE_KEY_HASH")
}
